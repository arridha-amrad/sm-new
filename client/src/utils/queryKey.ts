const queryKeys = {
  // user
  me: "/api/user/me",
  // post
  posts: "/api/post",
  // notifications
  notifs: "/api/notif",
  // conversations
  conversations: "/api/chat/many",
};

export default queryKeys;
